# Binaries
/bin/
*.exe
*.out

# Env files (never commit secrets / Ollama API keys)
.env
.env.local

# Go
*.test
*.prof
coverage.out
coverage.html

# OS / editor
.DS_Store
*.swp

# Local tooling / agent workspace (not part of the deliverable)
.claude/
